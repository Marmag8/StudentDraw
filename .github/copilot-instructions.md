# Copilot Instructions

## Wytyczne projektu
- User corrected that the title label is visible on gray background with white text, implying the CSS color rule for `#titlebar-title` is not being applied while `.titlebar` is applied.